<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appdatmon">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppDatMon"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Launcher -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.User.OrderHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.BookingHistoryActivity"
            android:exported="false" />

        <!-- Huy -->
        <activity
            android:name=".ui.admin.AdminActivity"
            android:exported="false" />

        <activity
            android:name=".ui.staff.StaffActivity"
            android:exported="false" />

        <activity
            android:name=".ui.admin.KitchenActivity"
            android:exported="false" />



        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ActivityAdmin"
            android:exported="false" />

        <!-- Thinh -->
        <activity
            android:name=".ui.User.BookingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.ScanQRActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.MenuActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.CartActivity"
            android:exported="false" />


        <activity
            android:name=".ui.User.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.ReviewActivity"
            android:exported="false" />

        <!-- Menu -->
        <activity
            android:name=".ui.User.StoryActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.ChefActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.FoodActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.SpaceActivity"
            android:exported="false" />

        <activity
            android:name=".ui.User.ContactActivity"
            android:exported="false" />

    </application>

</manifest>
